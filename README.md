# 🚀 Academic-Video-Notes-AI (学术视频/播客收割机) 🎓

这是一个专为**深度学习者**设计的自动化工具。它能一键将 B站、播客或 YouTube 的长视频/音频转化为**带原文金句的思维导图逻辑框架**。



## ✨ 项目亮点
- 🎙️ **精准听写**：调用 OpenAI Whisper 模型，即使是嘈杂的播客也能准确转录。
- ✍️ **深度拆解**：不仅提供小标题，更自动剥离出**子观点**与**原文金句「」**。
- 🔗 **全能支持**：支持 Bilibili、小宇宙 FM、YouTube 等几乎所有主流平台。
- 📂 **即拿即用**：自动导出 `.txt` 全文和 `.md` 思维导图大纲。

---

## 🛠️ 工作流说明
音频抓取：使用 yt-dlp 提取最高音质音频。

语音转文字：Whisper (Base) 模型进行全文转录。

逻辑剥离：通过 qwen-plus (通义千问) 提取深度逻辑框架。

自动下载：任务完成后，浏览器会自动弹出两个下载任务。

## 📝 进阶技巧
生成的 .md 文件可以完美导入 幕布 (Mubu)、XMind 或 Obsidian。

建议将 .md 文件直接拖入 XMind，瞬间生成一张逻辑严密的知识地图。

📝 进阶技巧：二阶段精炼 (Gemini/GPT-4o)
虽然本项目支持切片处理，但为了获得“既有大局观、又有细节肉感”的笔记，强烈建议将导出的 最终收割全稿.txt 喂给 Gemini 1.5 Pro 或 GPT-4o 进行二次加工。
💡 核心策略：保留原文血肉
如果你不希望 AI 破坏原有的逻辑美感，请使用以下针对性 Prompt：
推荐 Prompt： “这是我通过 Whisper 提取的一段学术讲座全稿。内容非常硬核，请不要对我进行过度解构或总结。
              你的任务是：
              分段标点化：仅在不改变原意的前提下，进行合理的逻辑分段。
              保留原文：严禁大幅度删减原文词句，我需要看到完整的论证细节。
              高亮核心：仅对每段中的核心金句或关键结论进行加粗处理。
              目标是给我一份排版得体、逻辑清晰且原汁原味的阅读脚本。”




## 🤝 贡献与反馈
如果你觉得这个工具有点意思，欢迎给个 Star ⭐！



📂 本地文件处理模式 (Local Upload Mode)

当视频链接失效、被 B 站封锁 IP (412 错误) 或视频文件过大时，推荐使用本地上传。

准备文件：在左侧文件夹图标处，将你的视频/音频文件直接拖入 Colab。

复制路径：上传完成后，右键点击文件名选择 Copy path (路径通常为 /content/你的文件名)。

开始收割：将路径粘贴进工具的“输入区域”即可。

⚡ 性能调优 (Performance Tips)

🚀 极速上传建议 (强烈推荐)： 如果你计划处理本地大视频，请务必先在本地将其转换为 MP3 格式再上传。
  
  原因：视频文件（如 .mp4）通常几百 MB，上传极慢且容易导致 Colab 连接中断；转换为 MP3 后仅需几 MB。
  
  工具：推荐使用 [格式工厂]、[ffmpeg] 或任何在线视频转音频工具。

🔥 开启 GPU 加速： 在 Colab 菜单栏选择 Runtime -> Change runtime type -> 硬件加速器选 T4 GPU。 开启后，1小时音频处理时间将从 40min 缩短至 5min 左右。

🛡️ 应对 Bilibili 412 封锁： 如果遇到 HTTP Error 412: Precondition Failed，说明 Colab 当前 IP 被 B 站临时屏蔽。

   对策：改用上述“本地文件处理模式”，先下载音频再上传。

🧩 断点续传说明： 本工具具备自动存盘功能。如果处理中途崩溃，只需重新点击“立即处理”，程序会自动跳过已完成的片段（Part_X.txt），实现秒速断点续传。


## ⚡ 极速开始 (Colab 一键运行)

不需要安装任何环境，直接在 [Google Colab](https://colab.research.google.com/) 新建一个单元格，粘贴并运行以下代码：

```python
# 1. 强制清理环境（确保没有任何旧的坏文件干扰）
import os, glob
files_to_delete = ['main.py', 'temp_audio_*.mp3', 'temp_audio_*.m4a', 'raw_*.txt', 'temp_*_P*.mp3']
for pattern in files_to_delete:
    for f in glob.glob(pattern):
        try:
            os.remove(f)
            print(f"🧹 已清理旧文件: {f}")
        except:
            pass

# 2. 环境安装（新增了 pydub 和 ffmpeg）
print("🛠️ 正在安装工业级收割组件 (Whisper, Gradio, Pydub, FFmpeg)...")
!pip install -q openai-whisper openai yt-dlp gradio pydub
!apt-get install -y ffmpeg

# 3. 重新下载 main.py
print("📥 正在从 GitHub 同步最新的【工业流水线版】代码...")
!curl -L -o main.py https://raw.githubusercontent.com/Andyaoao123/Academic-Video-Notes-AI/main/main.py

# 4. 验证并启动
if os.path.exists('main.py'):
    print("✅ 工业流水线版同步成功！正在启动界面...")
    import main, importlib
    importlib.reload(main)
    # 这一步会自动弹出手机可用的 .gradio.live 链接
    main.demo.launch(share=True, debug=True)
else:
    print("❌ 严重错误：main.py 下载失败，请检查 GitHub 链接。")










